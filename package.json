{
  "name": "telegram-sanitizer-worker",
  "private": true,
  "version": "1.0.0",
  "description": "Telegram webhook bot on Cloudflare Workers: sanitize + repost + delete original",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
